export enum sectionTypes {
  information,
  badgeList,
}

export class resourceUrls {
  static readonly portfolioImageUrl = '/assets/portfolio/art.json';
  static readonly resumeJsonUrl = '/assets/content/resume.json';
}